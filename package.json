{
  "license": "MIT",
  "scripts": {
    "prod": "docker-compose -f prod.docker-compose.yml up -d",
    "prod:down": "docker-compose -f prod.docker-compose.yml down",
    "prod:clean": "docker-compose -f prod.docker-compose.yml down -v"
  }
}
