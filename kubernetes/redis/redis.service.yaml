apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: rollmein
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: rollmein
    tier: backend
  ports:
    - port: 6379
      name: redis
      targetPort: 6379
