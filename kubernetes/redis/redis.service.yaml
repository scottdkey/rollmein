apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: rollmein
    tier: redis
spec:
  type: ClusterIP
  selector:
    app: rollmein
    tier: redis
  ports:
    - port: 6379
      name: redis
      targetPort: 6379
